# ONPRINT

ONPRINT is a full-stack platform for a professional printing-services business: a public marketing site, a customer portal for quotes/orders, and an admin panel — built for production deployment on traditional GoDaddy/cPanel hosting powered by Node.js and MySQL.

## Database & Tech Stack

- **Client:** React 19, Vite, React Router, Tailwind CSS, Axios
- **Server:** Node.js, Express, `mysql2` (promise pool)
- **Database:** GoDaddy MySQL / MariaDB (relational schema in `onprintdb.sql`)
- **Auth:** JWT + bcrypt password hashing

## Project Structure

```
onprint/
├── package.json               Production Node app with express, mysql2, bcryptjs, jsonwebtoken, helmet, cors
├── server.js                  Entry point — binds process.env.PORT
├── onprintdb.sql  Full MySQL schema & initial seed data for GoDaddy
├── src/
│   ├── config/database.js     Centralized mysql2 connection pool & connection tester
│   ├── controllers/           authController, productController, categoryController, serviceController, quoteController, orderController, contactController, newsletterController
│   ├── routes/                authRoutes, productRoutes, categoryRoutes, serviceRoutes, quoteRoutes, orderRoutes, contactRoutes, newsletterRoutes
│   ├── middleware/            auth.js (JWT verify + admin check), errorHandler.js
│   ├── data/                  initialData.js
│   └── app.js                 Express app setup & router mounting
├── client/                    React + Vite frontend
├── dist/                      React production build (generated by `npm run build`)
├── .env.example
└── README.md
```

## GoDaddy MySQL Database Setup

1. **Create MySQL Database in GoDaddy / cPanel**:
   - Go to cPanel -> **MySQL Database Wizard**.
   - Create a database (e.g. `onprint_db`) and database user. Assign full privileges.
2. **Import SQL Schema**:
   - Open cPanel -> **phpMyAdmin**.
   - Select your database and click **Import**.
   - Upload `onprintdb.sql` and run.
3. **Configure Environment Variables (`.env`)**:
   ```env
   DB_HOST=YOUR_GODADDY_MYSQL_HOST
   DB_PORT=3306
   DB_NAME=YOUR_GODADDY_DATABASE_NAME
   DB_USER=YOUR_GODADDY_DATABASE_USER
   DB_PASSWORD=YOUR_GODADDY_DATABASE_PASSWORD
   JWT_SECRET=your_jwt_secret_key_here
   NODE_ENV=production
   ```
4. **Deploy Node.js App on GoDaddy**:
   - Go to cPanel -> **Setup Node.js App**.
   - Set Application Root to project root, Application Startup File to `server.js`.
   - Run `npm install` and `npm run build`.
   - Start the application.

## Local Development

```bash
npm install
cp .env.example .env
npm run dev:all
```

- Client: `http://localhost:5173`
- API: `http://localhost:5000/api` (Health check: `/api/health`)

## Verification Endpoint

- `GET /api/health` returns:
  ```json
  {
    "success": true,
    "database": "mysql",
    "databaseConnected": true,
    "message": "ONPRINT GoDaddy MySQL API is running"
  }
  ```
